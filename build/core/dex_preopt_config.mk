# Apply all project to build Chrome with 'speed'
DEXPREOPT.$(TARGET_PRODUCT).Chrome.CONFIG := --compiler-filter=speed

# Apply all project to build Google maps with 'speed'
DEXPREOPT.$(TARGET_PRODUCT).Maps.CONFIG := --compiler-filter=speed

# Apply all project to build UserTrialFeedback with 'speed'
DEXPREOPT.$(TARGET_PRODUCT).UserTrialFeedback.CONFIG := --compiler-filter=speed

# Apply all project to build MirrorLink_MirrorLinkService with 'speed'
DEXPREOPT.$(TARGET_PRODUCT).MirrorLink_MirrorLinkService.CONFIG := --compiler-filter=speed

# Apply all project to build Camera with 'speed'
DEXPREOPT.$(TARGET_PRODUCT).Camera.CONFIG := --compiler-filter=speed
DEXPREOPT.$(TARGET_PRODUCT).Camera2.CONFIG := --compiler-filter=speed

